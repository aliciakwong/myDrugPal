package com.example.mydrugpal;

import android.content.Context;

import androidx.appcompat.app.AppCompatActivity;

/**
 * At the moment this class does nothing.
 * LogIntakeNotificationHelper and LogIntakeNotificationReceiver are doing the work.
 * Alarm is launched at startup on the SubstanceSummaryActivity.
 */

public class LogIntakeNotificationActivity extends AppCompatActivity {

    Context context = this;
}
